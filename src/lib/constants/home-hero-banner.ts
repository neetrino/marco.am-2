/** Settings row key — JSON payload validated by `home-hero-banner.schema`. */
export const HOME_HERO_BANNER_SETTINGS_KEY = "homeHeroBanner" as const;

/** Stored document version — bump when breaking shape changes. */
export const HOME_HERO_BANNER_VERSION = 1 as const;
