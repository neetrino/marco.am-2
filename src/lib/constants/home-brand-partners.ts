/** `settings.key` for home brand partners strip (curated order + section titles). */
export const HOME_BRAND_PARTNERS_SETTINGS_KEY = "homeBrandPartners";

export const HOME_BRAND_PARTNERS_STORAGE_VERSION = 1 as const;

/** Max curated partner rows stored in settings (UI rail shows a subset per layout). */
export const HOME_BRAND_PARTNERS_MAX_ENTRIES = 48;
