/** Settings row key — JSON payload validated by `why-choose-us.schema`. */
export const WHY_CHOOSE_US_SETTINGS_KEY = "homeWhyChooseUs" as const;

/** Stored document version — bump when breaking shape changes. */
export const WHY_CHOOSE_US_STORAGE_VERSION = 1 as const;
